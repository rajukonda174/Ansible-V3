- name: Jinja templates
  hosts: localhost
  connection: local
  vars:
    x: 100
  tasks:
    - name: copy
      copy:
        src: sample.txt
        dest: /tmp/s1.txt

    - name: template
      template:
        src: sample.txt
        dest: /tmp/s2.txt