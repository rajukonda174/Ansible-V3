- name: Install MySQL Client
  ansible.builtin.dnf:
    name: mysql8.4
    state: present

- name: Load Schema
  ansible.builtin.shell: mysql -h 10.0.0.11 -u root -pRoboShop@1 < db/schema.sql
  args:
    chdir: /app

- name: Load App User
  ansible.builtin.shell: mysql -h 10.0.0.11 -u root -pRoboShop@1 < db/app-user.sql
  args:
    chdir: /app